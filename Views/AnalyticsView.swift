import SwiftUI

struct AnalyticsView: View {
    var body: some View {
        NavigationStack {
            Text("Analytics")
                .navigationTitle("Analytics")
        }
    }
}

#Preview {
    AnalyticsView()
}
